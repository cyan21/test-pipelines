resources:
  - name: src_code_test_podman
    type: GitRepo
    configuration:
      gitProvider: yann_github
      path: cyan21/test-pipelines
      branches:
        include: master
      buildOn:
         commit: false