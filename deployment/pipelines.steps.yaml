pipelines:
  - name: test_cleanup
    steps:
      - name: webapp_deploy_config
        type: deployment/Webapp
        configuration:
          technology: java
          appserver: jboss
          ansible:
            role: wildfly
            inventory: uat
          inputResources:
            - name: my_cron
          integrations:
            - name: artifactory_eu
        execution:
          onStart:
            - jfrog rt c show && jfrog --version
