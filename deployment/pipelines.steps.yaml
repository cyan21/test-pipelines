pipelines:
  - name: test_deployment
    steps:
      - name: deploy_webapp
        type: deployment/Webapp
        configuration:
          technology: java
          appServer: jboss
          ansible:
            role: wildfly
            inventory: uat
          inputResources:
            # - name: my_cron
            - name: vm_group
            - name: wh_jenkins
          integrations:
            - name: artifactory_eu
        execution:
          onStart:
            - jfrog rt c show && jfrog --version
